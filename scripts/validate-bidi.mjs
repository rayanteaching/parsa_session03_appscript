import fs from 'node:fs';let fail=0;const bad=m=>{fail++;console.error('FAIL '+m)},ok=m=>console.log('PASS '+m);const index=fs.readFileSync('Index.html','utf8'),scripts=fs.readFileSync('Scripts.html','utf8'),styles=fs.readFileSync('Styles.html','utf8');/dir="rtl"/.test(index)?ok('page rtl'):bad('page rtl missing');/dir="ltr"/.test(index+scripts)?ok('ltr code regions'):bad('ltr code missing');/direction:ltr/.test(styles)?ok('ltr code style'):bad('ltr code style missing');if(fail)process.exit(1);